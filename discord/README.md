# /discord

Role sync and education bot. Empty in Phase 1.

**Official vs community:** Official Discord bot API. Token lives only in host env. Guild allowlist, default deny.

**Phase 0 limits:** Human approval before any message send. Verify webhook signatures. Local models never receive the bot token. Do not call Discord in this phase.
